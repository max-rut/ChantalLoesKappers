---
type ServiceItem = {
  naam: string;
  prijs: string;
  vanafPrijs?: boolean;
};

export type ServiceCategorie = {
  slug: string;
  titel: string;
  intro?: string;
  items: ServiceItem[];
  notities?: string[];
};

interface Props {
  categorie: ServiceCategorie;
}

const { categorie } = Astro.props;
---

<article class="service-card" id={categorie.slug}>
  <header>
    <h3>{categorie.titel}</h3>
    {categorie.intro && <p class="intro">{categorie.intro}</p>}
  </header>

  <ul class="rows">
    {
      categorie.items.map((item) => (
        <li>
          <span class="name">{item.naam}</span>
          <span class="price">
            {item.vanafPrijs && <span class="from">vanaf</span>}
            {item.prijs}
          </span>
        </li>
      ))
    }
  </ul>

  {
    categorie.notities && categorie.notities.length > 0 && (
      <footer class="notes">
        {categorie.notities.map((n) => (
          <p>{n}</p>
        ))}
      </footer>
    )
  }
</article>

<style>
  .service-card {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-md);
    box-shadow: var(--shadow-sm);
  }

  .service-card h3 {
    margin: 0 0 0.25rem;
  }

  .intro {
    color: var(--color-text-muted);
    font-style: italic;
    margin: 0 0 var(--space-sm);
  }

  .rows {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .rows li {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: var(--space-md);
    padding: 0.75rem 0;
    border-bottom: 1px dashed var(--color-border);
  }

  .rows li:last-child {
    border-bottom: 0;
  }

  .name {
    flex: 1;
  }

  .price {
    font-variant-numeric: tabular-nums;
    font-weight: 500;
    white-space: nowrap;
  }

  .from {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-text-muted);
    font-weight: 400;
    margin-right: 0.4rem;
  }

  .notes {
    margin-top: var(--space-sm);
    padding-top: var(--space-sm);
    border-top: 1px solid var(--color-border);
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .notes p {
    margin: 0;
  }
</style>
